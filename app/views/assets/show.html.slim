- provide(:title, 'Sachanlage anzeigen')
h1
  | Sachanlage anzeigen

div = link_to icon(:copy, 'Kopieren'), new_asset_path(asset: @asset.as_blueprint), class: 'btn btn-default'
div = link_to icon(:copy, 'Löschen'), asset_path(@asset), method: :delete, data: { confirm: 'Bist Du sicher, dass Du diese Sachanlage löschen möchtest?' }, class: 'btn btn-default'

= render 'form'
